{
  "name": "layerledger-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix backend",
    "test": "npm test --prefix client && npm test --prefix backend",
    "lint": "npm run lint --prefix client && npm run lint --prefix backend",
    "build": "npm run build --prefix client"
  }
}
